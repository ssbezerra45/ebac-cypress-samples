{
  "name": "ebac-cypress-samples",
  "version": "1.0.0",
  "description": "Default Cypress Tests",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run --browser chrome",
    "report": "npx allure serve --clean allure-results",
    "report:generate": "npx allure generate --clean allure-results --output allure-report",
    "scripts": {
    "cy:run": "cypress run",
    "prereport": "mkdir -p public",
    "report": "cypress-mochawesome-reporter" 
}
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "allure-commandline": "^2.42.0",
    "cypress": "^15.1.0"
  },
  "devDependencies": {
    "allure-cypress": "^3.9.0"
  }
}
